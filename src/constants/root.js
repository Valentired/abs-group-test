const ROOT_PRODUCTS = document.getElementById("products");
const ROOT_CATALOG_MENU = document.getElementById("catalog_menu");
const ROOT_HEADER = document.getElementById("header");
const ROOT_SHOPPING = document.getElementById("shopping_cart");